spring:
  application:
    name: PMS
  datasource:
    url: jdbc:mysql://localhost:3306/pcmsr
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
server:
   port: 8082